# 海大网球订场 V2

相对 V1 的六项改造，逐条对应你提的需求。**V2.1 新增：Token 自动获取（内置抓包，不再需要 Fiddler）。**

---

## 零、Token 自动获取（V2.1）

页面上有两个入口，优先用方式一。

### 方式一 · 学号密码直接登录（最简单）

填学号 + 密码 → 点「🔑 登录并获取 token」。

走的是 H5 前端同款接口 `POST https://hdscs.hainanu.edu.cn/login`，
参数 `{username, password, loginType:"01"}`，与小程序**同一个后端**，token 通用。

- 不需要微信、不需要抓包、不需要证书
- 密码只用于这一次请求，**不保存**
- 失败会直接显示服务端原话（如「用户不存在/密码错误」）

### ⚠ 这个 /login 不是 CAS，门户是另一个

| 入口 | 地址 | 用的账号 |
|---|---|---|
| 统采统购平台（H5） | `https://hdscw.hainanu.edu.cn` | 打开后**自动跳 CAS** |
| **CAS 统一身份认证** | `https://authserver.hainanu.edu.cn/authserver/login` | 学号 + **门户密码**，首次需激活 |
| 平台自有账号接口 | `POST https://hdscs.hainanu.edu.cn/login` | 平台账号，**与 CAS 不是一套** |

实测结论：

- `handleLogin` 的源码是
  `async function r(l){ const {token:c} = await Fk(l); e.value=c; await i() }`
  ⇒ **password 明文传输，响应里 `token` 在顶层**，不存在"我们加密方式不对"的问题。
- 学号 + 门户密码 POST 到 `/login` 会返回 **「用户不存在/密码错误」** ——
  因为这个接口校验的是平台自有账号，CAS 登录页原话是
  「学生为学号，教工为工号，**首次使用需进行账号激活**」。
- hdscw **没有 SPA fallback**：只有 `/` 是 200，`/login`、`/login/pwd`、
  `/home` 全 404。所以浏览器打开后走的是前端路由 `/login`，
  而该页逻辑是无 token 就跳 CAS —— 你到不了那个账号密码页。

**推荐路径**：浏览器打开 `https://hdscw.hainanu.edu.cn` → 自动跳 CAS →
学号密码登录（必要时先激活）。**配合下面的 MITM 抓包，登录的同时 token 就到手了**，
不依赖 `/login` 认不认你的密码。

另有 `GET /ecard/login/<id>` 一卡通登录入口。

### 方式二 · 内置 MITM 抓包（不用装 Fiddler）

原理和 Fiddler 完全一样，只是内置了：

1. 自签一个本地 CA，装进**当前用户**的受信任根存储
2. 系统代理临时指向 `127.0.0.1:8899`
3. 微信小程序走系统代理 → 我们解密 → 从 `Authorization` 头里拿 token
4. 抓到后自动复原系统代理、把 token 写进配置

**操作步骤**

1. 先**关掉 Fiddler / Clash 等代理软件** —— 它们会持续把系统代理抢回去
   （页面右上角会显示当前代理，如果不是 `8899` 且带 ⚠ 就是被占了）
2. 点「安装证书」→ 弹出 Windows 确认框，**点【是】**（没看到去任务栏找）
3. 点「📡 开始抓包」
4. 去微信打开场地小程序，随便点两下
5. 抓到后自动停止并复原，token 自动填好

**安全边界**

- 只解密 `hdscs.hainanu.edu.cn` 和 `hdscw.hainanu.edu.cn`，其余流量原样转发
- 只读 `Authorization` 头，不记录请求体、不修改任何数据
- 不碰微信进程、不做注入，无封号风险
- 超时或异常一律复原系统代理

**可行性依据**：你用 Fiddler 抓到过明文请求，说明 PC 版微信小程序走系统代理且
信任系统根证书 —— Fiddler 做的事和这个模块完全一样。
已通过自闭环测试：拦截 → 动态证书 → 解密 → 捕获 token → 白名单外直连 → 端口释放。

**局限**

- 装证书必须手动点一次【是】（Windows 的安全设计，防恶意软件静默装根证书）
- 如果 Fiddler/Clash 在跑，代理设置可能被它们覆盖

### 为什么不用「读本地存储」

实测排除了：小程序数据目录
`%APPDATA%\Tencent\xwechat\radium\users\<uid>\applet\local\wx559b66d8c8ed12ec\`
下的 `usrmmkvstorage*` 是 **MMKV 且已加密**（可打印率 2%，全文件扫不到 JWT），
`__APP__.wxapkg` 头是 `V1MM`（微信 PC 版加密包），也解不开。

---

## 一、需求对照表

| # | 你的需求 | 实现位置 | 状态 |
|---|---|---|---|
| 1 | 每人每天只能订 1 小时 | `booker.SLOT_MINUTES=60` 硬锁 + 开抢前查「我的订单」+ 成功后全局熔断 | ✅ |
| 2 | 场地 1~7 可拖动排序 | 前端 `#courts` HTML5 拖放 + ↑↓ 微调，顺序写回 `config.court_order` | ✅ |
| 3 | 时间窗口 06:00–22:00，中间整点随便挑 | `enumerate_slots()` 生成窗口内所有整点 1 小时 | ✅ |
| 4 | 最快一秒/间隔多少能订几次 | 内置「📊 接口测速」，实测见下节 | ✅ |
| 5 | 查询空场是否消耗额度 | `getDay` 只读不写库 → 可高频侦察；下单才消耗 | ✅ |
| 6 | 加随机，但**不要用时间随机** | `RandomSource`：持久盐文件 + `os.urandom` + 个人口令 | ✅ |

---

## 二、速率实测（2026-09-15 23:20 于本机）

用 `config.json` 里的旧 token 打真实接口（该 token 已失效，服务端在鉴权层直接返回 401）：

| 接口 | 样本 | 平均 | 最快 | 最慢 | 折算 |
|---|---|---|---|---|---|
| `place/list` | 1 | 180 ms | — | — | — |
| `getDay`（查空场） | 30 次无间隔 | 111 ms | 98 ms | 140 ms | **9.4 次/秒** |
| `submitOrder`（下单） | 10 次 | 113 ms | 87 ms | 172 ms | ~8.8 次/秒 |

**关键观察：连续 30 次无间隔请求，全部正常返回，没有 429、没有超时、没有连接被拒**
⇒ 服务端**没有明显的 IP 级限流**。

**但要注意**：上面的数字是「401 鉴权短路」路径，服务端还没走到业务逻辑。
真实下单要查库存、写订单、调一卡通扣费，一定更慢。
所以页面里内置了「📊 接口测速」，**填入有效 token 后点一下就能拿到真实数据**，
它会自动给出推荐参数。

### 查询 vs 下单，哪个消耗额度？

| | `getDay` 查空场 | `submitOrder` 下单 |
|---|---|---|
| 性质 | 只读查询 | 写库 + 扣校园卡 |
| 消耗「每天 1 小时」额度 | **不消耗** | **消耗** |
| 能不能高频 | 能，可以反复侦察 | 不能，抢到即停 |

所以策略就是：**并发侦察（查）→ 挑出空场 → 串行精确打击（订）**。
侦察每 `refresh_rounds`（默认 4）个候选刷新一次，保证手上的空场图是新鲜的。

---

## 三、反脚本冲突：为什么不能用时间做随机

`random.seed(time.time())` 的致命问题：
**分发出去的脚本若在同一秒启动，随机序列完全相同** ——
于是所有人抢同一批时段，正面对撞，谁都抢不到。

V2 的 `RandomSource` 用三处熵叠加：

```
种子 = blake2b( 持久随机盐文件(首次 os.urandom 生成) + os.urandom(24) + 你的口令 )
```

- 不同机器 → 盐不同 → 序列不同
- 同一机器两次运行 → 混入 `os.urandom` → 序列也不同
- 想手动区分 → 在「个人口令」里填个暗号

页面右上角会显示你的**随机签**（如 `CEE0F120`），
每个人看到的都不一样，点「🎲 重新抽签」可以换一个身份。

随机化的对象是**候选尝试顺序**，不只是延迟：
- `court_rand`（默认）：场地按你拖的顺序，但每个场地里挑哪个小时是随机的
- `shuffle`：候选全部打乱，反冲突最强，但会牺牲你的场地偏好

---

## 四、怎么用

### 启动

```bat
cd src
python app_server.py
```

浏览器自动打开 `http://127.0.0.1:8081`（**注意是 8081，V1 占的是 8080**）。

或直接双击 `dist\HainanU_Tennis_Booking_V2.exe`。

### 步骤

1. **填 token** —— 见下一节
2. **选日期** —— 只能选「今天 +2 天」以内
3. **选时间窗口** —— 如 18:00–22:00，窗口内每个整点 1 小时都是候选
4. **拖动场地排序** —— 拖 ☰，或用 ↑↓；右侧开关可停用某片场地
5. **点「👀 预览空场」** —— 先看空场图和候选顺序，**不提交**
6. 确认无误后 **⚡ 立即抢单** 或 **⏰ 定时抢**（默认 07:59:58，卡 08:00 放号）

### 怎么抓 token

token 会过期，过期后页面会明确提示「token 已失效」。

1. 电脑版微信打开小程序，进入预订流程
2. 用 Fiddler / Charles 抓包，找任意一条 `hdscs.hainanu.edu.cn` 的请求
3. 复制请求头 `Authorization: Bearer ` 后面那一长串
4. 粘进页面，点「💾 保存配置」

> 提示：`place/list` 不需要 token 也能返回 7 片场地，
> 所以「预览能看到场地」**不等于** token 有效。
> 只有 `getDay` / `submitOrder` 才真的校验身份。

---

## 五、开放时段（2026-09-15 从 `place/list` 实测）

| 场地 | 周一~周四 | 周五 | 周六、周日 |
|---|---|---|---|
| 1号场 | 21:00–22:00 | 21:00–22:00 | 18:00–22:00 |
| 2~7号场 | 18:00–19:00、21:00–22:00 | 18:00–22:00 | 18:00–22:00 |

**周一~周四 19:00–21:00 是网球专业课，不对外开放** ——
选这个时段提交必然失败，不是被别人抢走了。

---

## 六、安全设计

- **一天一单保护**：开抢前查当天订单，有单直接拒绝，绝不重复扣费
- **提交一律串行**：同一账号并发下单会重复扣款，所以即使侦察是并发的，提交也是串行
- **成功即熔断**：抢到 1 单立刻停止，不会尝试任何其他候选
- **额度识别**：服务端返回「每人每天…」类文案时识别为 `limit_reached` 并停止
- **测速不成单**：测速用的是「2020-01-01 + 03:00-04:00」，绝不可能下单成功

> ⚠️ 下单 = 直接扣校园卡 20 元，无二次确认。
> Token 等同账号密码，请勿外传。自动化抢订可能违反小程序用户协议，风险自担。

---

## 七、文件说明

```
Tennis Booking V2/
├─ src/
│  ├─ booker.py       引擎：接口 / 规划 / 随机 / 速率 / 测速
│  ├─ app_server.py   本地服务（端口 8081）
│  ├─ index.html      前端（拖动排序在这里）
│  ├─ selftest.py     离线自检，改逻辑后跑一遍
│  └─ config.json     配置（含 token，已被 .gitignore 排除，不入库）
├─ config.example.json  配置模板：复制为 src/config.json 后填入 token
├─ build.bat          打包成 exe
└─ README.md
```

改完 `booker.py` 跑一次自检：

```bat
python src\selftest.py
```
